// export function tabWitiAccumulate<T>(accumulate: (acc: T, curr: T) => T, initialValue: T) {
//   return (source: Observable<T>) => {
//     return source.pipe(scan((acc, curr) => accumulate(acc, curr), initialValue))
//   }
// }
